"""Native refinement of the strongly asymmetric double-connection proposal.

Uses the same checked native two-parameter driver as the weak case. Explicit
parameter bounds and fixed saddle boxes prevent silent branch switching.
"""
from asymmetric_connection_probe import main
if __name__=='__main__':main('two-strongly-asymmetric-connections')
